<style>
  #sameline {
    overflow: hidden;
    white-space: nowrap;
    padding-top: 20px;
  }
</style>

<div id="sameline">
  <a href="/">Main Menu</a><a> --> </a>
  <a href="/customers/customers">Customers</a><a> --> </a>
  <a href="/customers/orderC">Order</a>
  <a> --> Entrees</a>
</div>

<div class="row mt-5">
    <div class="col-md-6 m-auto">
      <div class="card card-body text-center">
        <script src="https://translate.google.com/translate_a/element.js?cb=translateDropdown"></script>
          <script>
            function translateDropdown() {
              new google.translate.TranslateElement(
                {pageLanguage: 'en'}, 'google_translate_id'
              )
            }
          </script>
        <div id="google_translate_id"></div>
        <button class="btn btn-primary btn-block mb-2" onclick="history.back()">Return</button>
        <h1>Entrees</h1>
        <% for (var i =0; i < entreearr.length;  i++ ) { %>
          <form action = "/customers/orderC/entreesC/entree/<%= i %>" method = "POST"> 
          <div class="row">
                  <input type = "submit" value ="<%= entreearr[i].item %> : $<%= entreearr[i].price %>" />
                 </form>
              <% } %>
